<navbar>
    <div class="menu">
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_panneau_controle') }}">Panneau de contrôle</a>
        {% endif %}
        <a href="{{ path('app_home') }}">Accueil</a>
        <a href="{{ path('app_profil') }}">Mon Profil</a>
        <a href="{{ path('app_logout') }}">Se déconnecter</a>
    </div>
</navbar>